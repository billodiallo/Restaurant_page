import './style.css';
import headerPage from './header';

function creazy() {
    const i = 0;
    if (i < 0) {
      headerPage();
      renderHome();
      foodpage();
      contactPage();
    }
  }